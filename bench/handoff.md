# Handoff

State: hugegraph-single, hugegraph-cluster and neo4j complete for all
datasets and tests; cross-system FS/FN invariants agree exactly
(see results/). All three stopped.
Next: janusgraph (janusgraph/janusgraph:latest, BerkeleyJE) via
runner/janus.py, then final cleanup and summary.
